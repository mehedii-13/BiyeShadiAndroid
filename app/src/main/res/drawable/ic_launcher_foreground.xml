<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Adaptive icon canvas is 108x108dp, safe zone is center 72x72dp -->
    <!-- Add 18dp inset on all sides to keep logo in safe zone -->
    <item
        android:drawable="@drawable/app_logo"
        android:gravity="center"
        android:width="72dp"
        android:height="72dp"
        android:top="18dp"
        android:bottom="18dp"
        android:left="18dp"
        android:right="18dp" />
</layer-list>

